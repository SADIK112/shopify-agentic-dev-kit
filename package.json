{
  "name": "shopify-ai-os",
  "private": true,
  "version": "1.0.0",
  "description": "AI-first Shopify assistant repository for agentic developer tooling and skill routing.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "sync-skills": "node scripts/sync-skills.mjs",
    "check": "node scripts/check-architecture.mjs && npm run typecheck && npm run lint",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo 'This is the blueprint repo — run npm test from your Shopify app directory.' && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
